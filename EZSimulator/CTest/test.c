#include "stdio.h"

int main() {
	char o;
	char u;
	char x[3];
	char * y;
	char z;
	while(u != 2) {
		o = x[1];
		u = y[115];
		//y = x+1;
		y[0] = 5;
		y[115] = 2;
		scanf("%d", &z);
		printf("z=%d\n", z);
		printf("lol o=%d, u=%d\n", o, u);
		
	}
}